import 'package:flutter/widgets.dart';
import 'package:sahaame/utils/constants.dart';

Widget verticalSeparator({height = 10, color = lightBlueColor, width = 1}) {
  return Container(
    height: height,
    color: color,
    width: width,
  );
}
